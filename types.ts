export interface ModelInterface {
    address1: string;
    address2: string;
    city: string;
    coordinates: {
        lat: number;
        lng: number;
    };
    postalCode: string;
    state: string;
}
